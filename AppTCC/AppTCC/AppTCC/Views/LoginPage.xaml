<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="AppTCC.Views.LoginPage"
             Shell.NavBarIsVisible="True">
    
    <ContentPage.Content>
        
        <StackLayout>
            <Frame BackgroundColor="#000000">
                <ContentView VerticalOptions="FillAndExpand">
                    <Image Source="logofinv.png" VerticalOptions="FillAndExpand" />
                </ContentView>
            </Frame>
            <StackLayout Padding="50">
                <Label Text="Usuário" FontSize="Large" />
                <Entry Text="{Binding User, Mode=TwoWay}" FontSize="Large" />
                <Label Text="Senha" FontSize="Large" />
                <Editor Text="{Binding Password, Mode=TwoWay}" AutoSize="TextChanges" FontSize="Large" Margin="0" />
                <StackLayout  VerticalOptions="Center">
                    <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}" FontSize="Large"/>
                </StackLayout>
            </StackLayout>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>